<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Temporarily comment or skip the broken xpath to allow installation -->

    <!-- This is just a placeholder for now. You can edit later via UI or Studio. -->
    <record id="sale_order_form_line_tree_inherit_warehouse" model="ir.ui.view">
        <field name="name">sale.order.form.dummy.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <!-- Safe dummy modification -->
            <xpath expr="//field[@name='order_line']" position="attributes">
                <attribute name="string">Order Lines (Customized)</attribute>
            </xpath>
        </field>
    </record>
</odoo>
